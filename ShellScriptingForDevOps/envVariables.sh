#! /bin/bash

#while running shell scripts, you can't straight forward access variables set in the terminal
#there is a need to export them into global environment variables
#USERNAME="admin"   |   all caps convention
#export USERNAME=$USERNAME
